# SSH alias from your ~/.ssh/config file
DEPLOY_SSH_ALIAS=vps
# Server Configuration
DEPLOY_PATH=/home/user/apps/vpn
SERVICE_NAME=sing-box
# sudo password (if passwordless sudo is not configured)
DEPLOY_SUDO_PASSWORD=your_password
